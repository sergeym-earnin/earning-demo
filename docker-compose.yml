version: '3'

services:
  earning.demo.api:
    image: earning.demo.api
    build:
      context: ./Earning.Demo.Api
      dockerfile: Dockerfile
    ports:
      - "8080:49160"

  earning.demo.web:
    image: earning.demo.web
    build:
      context: ./Earning.Demo.Web
      dockerfile: Dockerfile
    ports:
      - "80:49161"

  earning.demo:
    image: earning.demo
    build:
      context: ./Earning.Demo
      dockerfile: Dockerfile
